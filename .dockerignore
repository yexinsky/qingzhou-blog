# 构建上下文裁剪：只保留构建镜像真正需要的源码与配置。
# 注意不要把 drizzle/（迁移文件）、src/、package-lock.json、docker/ 排除掉。
node_modules
.next
.git
.gitignore

# 运行时数据与产物，绝不进镜像
uploads
backups
coverage
*.log
*.tsbuildinfo

# 本地环境变量：镜像内一律由 compose 注入，避免开发配置被带进生产镜像
.env
.env.*
*.local

# 主机侧编排文件与文档
docker-compose*.yml
PRD_*.md
README.md

# 测试与调试产物
__tmp_*
*-evidence/
.playwright-mcp/
tasks/
test-results/
