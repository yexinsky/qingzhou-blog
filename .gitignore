# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files
.env
.env.*
!.env.example
!.env.docker.example
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# security-sensitive/local runtime artifacts
*.log
logs/
*.dump
*.sql
# 例外：drizzle 迁移脚本是项目源码，必须入库（否则容器构建/部署会缺迁移文件）
!drizzle/*.sql
*.sqlite
*.sqlite3
*.bak
*.backup
*.tgz
*.zip
playwright-report/
test-results/

# temporary debugging scratch files (local QA sessions)
__tmp_*
.playwright-mcp/
test.txt
tasks/dbcheck*.cjs
tasks/capture.mjs
tasks/darktest.mjs
tasks/layout-analyze.mjs
tasks/build_final.txt
tasks/capture_summary.json
tasks/verify-screenshots/

# 测试证据与调试产物（规范化仓库，不入库）
gui-test-screenshots/
*-evidence/
phase*.png
phase*.log
verify-*.png
audit*.json
security-audit-*.json
admin-test-evidence-*/

# 附件本地存储与备份产物（运行时生成）
/uploads/
/backups/
# compose 部署时的数据目录（MySQL 数据文件等，绝不能入库）
/data/
